# MCU FFT 上板与交接指南

更新时间：2026-07-04

本文是仓库当前唯一建议阅读的上板交接文档，已合并旧的状态报告、上板前检查记录、老师强调的资源统计口径、后续操作指南和本次 Vivado bitstream 结果。

## 1. 当前结论

推荐首板路线：

```text
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul
```

本路线已经完成：

| 项目 | 结果 |
| --- | --- |
| 本地 Verilog 功能回归 | PASS |
| Vivado 综合 | PASS |
| Vivado 实现 | PASS |
| DRC | 0 Error |
| 无 ILA bitstream | 已生成 |
| 带 ILA bitstream 和 LTX | 已生成 |
| MCU 内 DSP 使用 | 0 |
| 板上实际 MCU 时钟 | 130 MHz |
| 130 MHz 上板验证 | PASS |

当前目标板卡 part：

```text
xc7k160tffg676-2
```

确认依据：课程课件 `materials/source_docs/Lab1.pdf` 写明实验板 FPGA 为 `XC7K160T-2FFG676-I`，Vivado 选型为 `xc7k160tffg676-2`。因此本次已将 Vivado 脚本默认 part 校正为 K160/FFG676，并用该器件重新完成综合、实现、DRC 和 bitstream。上板前仍建议看一次 FPGA 丝印；如果实物丝印与 K160/FFG676 不一致，必须重新核对 XDC。

## 2. 老师强调项落实记录

| 要求 | 当前落实情况 |
| --- | --- |
| 资源统计时 `flatten_hierarchy` 设为 `none` | `run_board_bitstream.tcl`、`create_board_project.tcl`、矩阵脚本和综合冒烟脚本均已默认设置 `SYNTH_FLATTEN_HIERARCHY=none` |
| MCU 不使用 DSP | Vivado 综合参数已默认设置 `SYNTH_MAX_DSP=0`，最新无 ILA 和带 ILA 报告中 `DSPs=0` |
| 资源报告关注 MCU 内部，不把测试平台算进去 | 已生成 `board_utilization_hierarchical.rpt` 和 `board_utilization_hierarchical_no_ila.rpt`，资源表中可单独查看 `u_mcu_top` |
| 不使用专用 FFT/排序/外设加速器 | 推荐路线 RTL 未例化 FFT IP、排序 IP、DMA/AXI 外设加速器；FFT 由 MCU 执行指令 ROM 中的程序完成 |
| 不扩展 ARM 指令集范围外的指令 | 当前设计仍由原 MCU/ALU/控制器执行既有指令流程，未额外挂接专用在线计算模块 |

## 3. 已修复的问题

| 问题 | 处理 |
| --- | --- |
| Vivado 缺目标器件/license | 已补齐，综合和实现均可获取 `xc7k160t` license |
| 目标 part 与课件实物封装不一致 | 默认 part 改为 `xc7k160tffg676-2` |
| KEY1 所在 HP Bank 不支持 `LVCMOS33` | KEY1 改为 `LVCMOS18` |
| `$readmemh` 在 Vivado run 目录找不到 `mem/*.mem` | 综合前自动复制 `mem/*.mem` 和 `mem/*.coe` |
| OneDrive/中文/长路径影响 Vivado ILA 工程 | 支持 `OUT_DIR`，建议把 Vivado build 放到 `D:/vivado_work/...` |

## 4. 已生成的交付物

当前最终板上保留的是 130 MHz PLL 无 ILA 正式版本；带 ILA 版本只用于功能抓波。130 MHz 交付物位于本机：

```text
output/hardware_debug/routeA_130MHz_PLL_20260704/no_ila/board_top_130pll_no_ila.bit
output/hardware_debug/routeA_130MHz_PLL_20260704/ila/board_top_130pll_ila.bit
output/hardware_debug/routeA_130MHz_PLL_20260704/ila/board_top_130pll_ila.ltx
output/hardware_debug/routeA_130MHz_PLL_20260704/ila/routeA_130pll_verify_we_capture.csv
output/pdf/RouteA_130MHz_PLL_board_report.pdf
```

说明：`output/` 是本机交付物目录，按 `.gitignore` 不提交到 GitHub；GitHub 保留源码、脚本和报告摘要。

历史 50 MHz 版本文件仍保留：

```text
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.bit
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.ltx
```

关闭 ILA 的资源成绩版本：

```text
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_no_ila.bit
```

报告文件：

```text
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_timing_summary.rpt
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_utilization.rpt
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_utilization_hierarchical.rpt
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_drc.rpt
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_methodology.rpt
routesA/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_bitstream_status.txt
```

无 ILA 版本对应报告均带 `_no_ila` 后缀，例如 `board_utilization_no_ila.rpt`。

## 5. 最新实现摘要

当前顶层已加入 `PLLE2_BASE`，将板载 `CLK_50M` 倍频到 130 MHz。PLL 参数为 `CLKFBOUT_MULT=26`、`CLKOUT0_DIVIDE=10`，复位逻辑为 `rst = ~KEY1 | ~pll_locked`。

| 指标 | 130 MHz 无 ILA 正式版 | 130 MHz 带 ILA 验证版 |
| --- | ---: | ---: |
| WNS | 0.190 ns | -0.033 ns |
| TNS | 0.000 ns | 0.000 ns |
| WHS | 0.205 ns | 0.059 ns |
| THS | 0.000 ns | 0.000 ns |
| Slice LUTs | 998 | 约 2300 |
| Slice Registers | 549 | 约 2800 |
| Block RAM Tile | 0 | 2 |
| DSPs | 0 | 0 |
| BUFGCTRL | 2 | 3 |
| PLLE2_ADV | 1 | 1 |
| DRC Error | 0 | 0 |

层级资源中 MCU 本体 `u_mcu_top`：

| 层级 | LUT | FF | BRAM | DSP |
| --- | ---: | ---: | ---: | ---: |
| `u_mcu_top` 无 ILA | 952 | 549 | 0 | 0 |
| `u_mcu_core` 无 ILA | 798 | 530 | 0 | 0 |
| `u_alu` 无 ILA | 366 | 0 | 0 | 0 |
| `u_reg_file` 无 ILA | 264 | 512 | 0 | 0 |

说明：正式资源成绩建议使用无 ILA 报告；带 ILA 版本只用于首板抓波形，ILA 自身会额外消耗 LUT、FF 和 BRAM，并可能轻微影响时序。当前带 ILA 版本 WNS 为 `-0.033 ns`，仅作为功能抓波依据；最终上板状态使用无 ILA 版本。

DRC warning 当前主要包括 `CFGBVS/CONFIG_VOLTAGE` 未显式设置；带 ILA 版本还包含 ILA/调试网络相关 warning，不阻塞 bitstream。若老师要求完全清 warning，需要根据板卡配置电压补充对应 XDC 属性。

## 6. 上板步骤

1. 打开 Vivado Hardware Manager。
2. 连接 K7EDAEVAL，执行 `Open Target -> Auto Connect`。
3. Program Device，选择 `board_top_ila.bit`。
4. 同时加载 `board_top_ila.ltx`。
5. 打开 ILA，观察 `done`、`verify_we`、`verify_addr`、`verify_vector_out` 和 `cnt_test`。

判断标准：

| 信号 | 期望 |
| --- | --- |
| `done` | 最终拉高 |
| `verify_we` | 出现 16 次有效写入 |
| `verify_addr` | 最后写地址为 15 |
| `cnt_test` | 接近 157 |
| `verify_vector_out` | 写出顺序与 `mem/FFT_output.coe` 一致 |

LED 映射：

| LED | 信号 |
| --- | --- |
| LED1 | `done` |
| LED2 | `verify_we` |
| LED3 到 LED7 | `cnt_test` 部分位 |
| LED8 | `verify_RAM` debug 数据异或值 |

ILA probe：

| probe | 信号 |
| --- | --- |
| `probe0` | `test_vector_in[15:0]` |
| `probe1` | `verify_vector_out[15:0]` |
| `probe2` | `verify_we` |
| `probe3` | `verify_addr[4:0]` |
| `probe4` | `cnt_test[19:0]` |
| `probe5` | `done` |

## 7. 重新生成 bitstream

建议先映射短路径：

```powershell
subst M: C:\Users\戎择辰\OneDrive\文档\数电实验\MCU
cd M:\routesA\speed_v7_q7_narrow_mul\mcu_fft_q7_narrow_mul
```

无 ILA 版本：

```powershell
@'
set ENABLE_ILA 0
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k160tffg676-2
set SYNTH_FLATTEN_HIERARCHY none
set SYNTH_MAX_DSP 0
set JOBS 4
set OUT_DIR D:/vivado_work/mcu_q7_k160_no_dsp_no_ila
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```

带 ILA 版本：

```powershell
@'
set ENABLE_ILA 1
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k160tffg676-2
set SYNTH_FLATTEN_HIERARCHY none
set SYNTH_MAX_DSP 0
set JOBS 4
set OUT_DIR D:/vivado_work/mcu_q7_k160_no_dsp_ila
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```

## 8. 时钟说明

当前 `rtl/board_top.v` 使用 `PLLE2_BASE` 将板载 50 MHz 转换为 130 MHz：

```verilog
clk_130_pll u_clk_130_pll (
    .clk_in(CLK_50M),
    .clk_out(clk),
    .locked(pll_locked)
);

assign rst = ~KEY1 | ~pll_locked;
```

因此当前下载到板上的真实 MCU 运行频率是 130 MHz。`cnt_test=157` 时，测试窗口耗时约 `157 / 130 MHz = 1.208 us`。

## 9. 后续可选优化

1. 核对板卡配置电压后，在 XDC 中补充 `CFGBVS` 和 `CONFIG_VOLTAGE`。
2. 用 `board_utilization_hierarchical_no_ila.rpt` 作为资源统计依据，记录 `u_mcu_top` 而不是整个测试平台。
3. 若要继续追求频率，可运行 `routesA/speed_v8_route_a_vivado_matrix` 的矩阵脚本比较 v6/v7/v7b/v7c。
4. 若常数 91 专用路线没有明显时序优势，建议保留 `speed_v7_q7_narrow_mul`，因为它语义更通用、风险更低。

## 10. 下一步操作顺序

建议按下面顺序推进，不要直接跳到无 ILA 版本：

1. 首次上板使用 `board_top_ila.bit` 和 `board_top_ila.ltx`，确认 ILA 中 `done` 拉高、`verify_we` 有 16 次有效写入、`verify_addr` 最后到 15。
2. 对照 `mem/FFT_output.coe` 检查 `verify_vector_out` 的 16 个输出值；若输出不一致，先保留带 ILA 版本继续抓波形。
3. 带 ILA 版本确认功能无误后，再切换到 `board_top_no_ila.bit` 做正式资源、速度和演示记录。
4. 正式资源表使用 `board_utilization_hierarchical_no_ila.rpt`，优先记录 `u_mcu_top` 层级，不把 `test_ROM`、`verify_RAM`、ILA 调试平台算入 MCU 本体资源。
5. 路线选择参考 `routesA/speed_v8_route_a_vivado_matrix/results/leaderboard_summary.md`：若强调效率，优先看 `speed_v7c_c91_shift_sub`；若强调通用性和风险，保留 `speed_v7_q7_narrow_mul`。

关闭 ILA 不会改变 MCU 计算逻辑，只会移除在线波形抓取能力。调试阶段用带 ILA，提交资源和速度成绩时用无 ILA。
