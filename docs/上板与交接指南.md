# MCU FFT 上板与交接指南

更新时间：2026-07-03

本文是仓库当前唯一建议阅读的上板交接文档，已合并旧的状态报告、上板前检查记录、后续操作指南和本次 Vivado bitstream 结果。

## 1. 当前结论

推荐首板路线：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul
```

本路线已经完成：

| 项目 | 结果 |
| --- | --- |
| 本地 Verilog 功能回归 | PASS |
| Vivado 综合 | PASS |
| Vivado 实现 | PASS |
| DRC | 0 Error |
| 无 ILA bitstream | 已生成 |
| 带 ILA bitstream 和 LTX | 已生成 |

当前目标 part：

```text
xc7k325tffg676-2
```

说明：课程资料 `materials/source_docs/k7edaeval_pin_map.xlsx` 中的 CLK、KEY、LED 管脚与 `xc7k325tffg676-2` 匹配。旧文档中使用的 `xc7k325tffg900-2` 与这份引脚表不匹配，会出现多个无效 package pin。上板前仍要核对 FPGA 丝印；如果实物不是 FFG676，必须重新核对 XDC。

## 2. 已修复的问题

| 问题 | 处理 |
| --- | --- |
| Vivado 缺 K325 器件/license | 已补齐，综合和实现均可获取 license |
| 目标 part 与 XDC 管脚不匹配 | 默认 part 改为 `xc7k325tffg676-2` |
| KEY1 所在 HP Bank 不支持 `LVCMOS33` | KEY1 改为 `LVCMOS18` |
| `$readmemh` 在 Vivado run 目录找不到 `mem/*.mem` | 综合前自动复制 `mem/*.mem` 和 `mem/*.coe` |
| OneDrive/中文/长路径影响 Vivado ILA 工程 | 支持 `OUT_DIR`，建议把 ILA build 放到 `D:/vivado_work/...` |

## 3. 已生成的交付物

首板调试优先使用：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.bit
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.ltx
```

关闭 ILA 的版本：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_no_ila.bit
```

报告文件：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_timing_summary.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_utilization.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_drc.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_methodology.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_bitstream_status.txt
```

## 4. ILA 版实现摘要

约束周期：20.000 ns。

| 指标 | 结果 |
| --- | ---: |
| WNS | 9.217 ns |
| TNS | 0.000 ns |
| WHS | 0.044 ns |
| THS | 0.000 ns |
| Slice LUTs | 2163 |
| Slice Registers | 2790 |
| Block RAM Tile | 2 |
| DSPs | 1 |
| BUFGCTRL | 2 |
| DRC Error | 0 |

DRC warning 包括 `CFGBVS/CONFIG_VOLTAGE` 未显式设置、DSP pipeline 建议、ILA/调试网络相关提示，不阻塞 bitstream。正式资源成绩建议使用关闭 ILA 的实现结果。

## 5. 上板步骤

1. 打开 Vivado Hardware Manager。
2. 连接 K7EDAEVAL，执行 `Open Target -> Auto Connect`。
3. Program Device，选择 `board_top_ila.bit`。
4. 同时加载 `board_top_ila.ltx`。
5. 打开 ILA，观察 `done`、`verify_we`、`verify_addr`、`verify_vector_out` 和 `cnt_test`。

判断标准：

| 信号 | 期望 |
| --- | --- |
| `done` | 最终拉高 |
| `verify_we` | 出现 16 次有效写入 |
| `verify_addr` | 最后写地址为 15 |
| `cnt_test` | 接近 157 |
| `verify_vector_out` | 写出顺序与 `mem/FFT_output.coe` 一致 |

LED 映射：

| LED | 信号 |
| --- | --- |
| LED1 | `done` |
| LED2 | `verify_we` |
| LED3 到 LED7 | `cnt_test` 部分位 |
| LED8 | `verify_RAM` debug 数据异或值 |

ILA probe：

| probe | 信号 |
| --- | --- |
| `probe0` | `test_vector_in[15:0]` |
| `probe1` | `verify_vector_out[15:0]` |
| `probe2` | `verify_we` |
| `probe3` | `verify_addr[4:0]` |
| `probe4` | `cnt_test[19:0]` |
| `probe5` | `done` |

## 6. 重新生成 bitstream

建议先映射短路径：

```powershell
subst M: C:\Users\戎择辰\OneDrive\文档\数电实验\MCU
cd M:\routes\speed_v7_q7_narrow_mul\mcu_fft_q7_narrow_mul
```

无 ILA 版本：

```powershell
@'
set ENABLE_ILA 0
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k325tffg676-2
set JOBS 4
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```

带 ILA 版本：

```powershell
@'
set ENABLE_ILA 1
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k325tffg676-2
set JOBS 4
set OUT_DIR D:/vivado_work/mcu_q7_ila
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```

## 7. 时钟说明

当前 `rtl/board_top.v` 直接使用板载 50 MHz：

```verilog
assign clk = CLK_50M;
```

因此下载到板上后的真实运行频率是 50 MHz。路线矩阵中的 95/100/110/120/130 MHz 是 Vivado 时序目标，用于判断 RTL 的收敛余量，不会自动生成真实高频时钟。如果需要板上实际高频运行，需要加入 PLL/MMCM 或外部高频时钟，并同步修改 `board_top.v` 和 XDC。

## 8. 后续可选优化

1. 核对板卡配置电压后，在 XDC 中补充 `CFGBVS` 和 `CONFIG_VOLTAGE`。
2. 关闭 ILA 后重新生成最终资源成绩版本。
3. 若要继续追求频率，可运行 `routes/speed_v8_route_a_vivado_matrix` 的矩阵脚本比较 v6/v7/v7b/v7c。
4. 若常数 91 专用路线没有明显时序优势，建议保留 `speed_v7_q7_narrow_mul`，因为它语义更通用、风险更低。
