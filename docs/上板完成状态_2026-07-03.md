# 上板完成状态记录（2026-07-03）

## 结论

当前已经补齐 Vivado 器件与 license，并完成推荐路线 `speed_v7_q7_narrow_mul` 在目标板卡 part 下的综合、实现、DRC 和 bitstream。

本轮验证使用的 part 为：

```text
xc7k325tffg676-2
```

原因：课程资料 `materials/source_docs/k7edaeval_pin_map.xlsx` 中的 CLK、KEY、LED 管脚在 `xc7k325tffg676-2` 下有效；同一套管脚在 `xc7k325tffg900-2` 下存在多个无效 package pin。上板前仍建议肉眼核对 FPGA 丝印；若实物封装不是 FFG676，必须重新核对 XDC。

## 已修复的阻塞项

| 项目 | 处理结果 |
| --- | --- |
| Vivado K325 器件包 | 已可识别 `xc7k325tffg676-2` |
| Vivado license | 综合与实现均可获取 license |
| `$readmemh` 初始化文件路径 | 已在综合前复制 `mem/*.mem`、`mem/*.coe` 到 Vivado run 工作目录 |
| package pin 不匹配 | 默认 part 从 `xc7k325tffg900-2` 调整为 `xc7k325tffg676-2` |
| KEY1 IOSTANDARD DRC | KEY1 从 `LVCMOS33` 调整为 `LVCMOS18` |
| OneDrive/长路径影响 ILA | `create_board_project.tcl` 支持 `OUT_DIR`，ILA 工程已在 `D:/vivado_work/mcu_q7_ila` 成功生成 |

## 当前生成物

首板推荐使用带 ILA 的调试文件：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.bit
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_ila.ltx
```

同时保留了关闭 ILA 的资源/速度比较版本：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_top_no_ila.bit
```

报告文件：

```text
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_timing_summary.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_utilization.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_drc.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_methodology.rpt
routes/speed_v7_q7_narrow_mul/mcu_fft_q7_narrow_mul/results/vivado_board/board_bitstream_status.txt
```

## ILA 版实现结果

| 指标 | 结果 |
| --- | ---: |
| 目标周期 | 20.000 ns |
| WNS | 9.217 ns |
| TNS | 0.000 ns |
| WHS | 0.044 ns |
| THS | 0.000 ns |
| Slice LUTs | 2163 |
| Slice Registers | 2790 |
| Block RAM Tile | 2 |
| DSPs | 1 |
| BUFGCTRL | 2 |
| DRC Error | 0 |

DRC 中仍有 warning，主要包括 `CFGBVS/CONFIG_VOLTAGE` 未显式设置、DSP pipeline 建议、ILA/调试网络相关提示。这些不阻塞 bitstream，但最终正式交付时可补充配置电压约束并关闭 ILA 重新生成资源成绩版本。

## 推荐上板步骤

1. 打开 Vivado Hardware Manager。
2. 连接 K7EDAEVAL，执行 `Open Target -> Auto Connect`。
3. Program Device 时选择 `board_top_ila.bit`。
4. 同时加载 `board_top_ila.ltx`。
5. 首轮观察 `done`、`verify_we`、`verify_addr`、`verify_vector_out`、`cnt_test`。

推荐判断标准：

| 信号 | 期望 |
| --- | --- |
| `done` | 最终拉高 |
| `verify_we` | 出现 16 次有效写入 |
| `verify_addr` | 最后写地址为 15 |
| `cnt_test` | 接近 157 |
| `verify_vector_out` | 写出顺序与 `mem/FFT_output.coe` 一致 |

## 重新生成命令

建议始终使用短路径映射：

```powershell
subst M: C:\Users\戎择辰\OneDrive\文档\数电实验\MCU
cd M:\routes\speed_v7_q7_narrow_mul\mcu_fft_q7_narrow_mul
```

关闭 ILA 的版本：

```powershell
@'
set ENABLE_ILA 0
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k325tffg676-2
set JOBS 4
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```

带 ILA 的版本建议把 Vivado 输出放到 OneDrive 外：

```powershell
@'
set ENABLE_ILA 1
set TARGET_PERIOD_NS 20.000
set PART_NAME xc7k325tffg676-2
set JOBS 4
set OUT_DIR D:/vivado_work/mcu_q7_ila
source ../../vivado/run_board_bitstream.tcl
'@ | D:\vivado\2025.2\Vivado\bin\vivado.bat -mode tcl -nolog -nojournal
```
