# V28 路线记录：控制与输出阶段开销检查

## 检查项

| 检查项 | 结果 |
| --- | --- |
| B/BEQ/BNE/BL | 不存在 |
| CMP | 不存在 |
| 循环结构 | 不存在 |
| HALT 前 NOP | 不存在 |
| HALT 是否影响 `cnt_test` | 不影响，计数在最后一次 verify 写入结束 |
| 输出写回是否能硬件合并 | 不能，规则要求 MCU 指令写回 |

## 实际优化

唯一有效改动是把输出基址 `VERIFY_BASE` 的初始化前移到计数窗口之外。该改动不改变 FFT 程序的数学顺序，也不改变输入/输出地址布局。

## 验证结果

| 项目 | 结果 |
| --- | ---: |
| 官方样例 + 20 随机 | PASS |
| `cnt_test` | 172 |
| 300 MHz timing | PASS |
| WNS/TNS | +0.067 ns / 0.000 ns |
| LUT/FF/DSP | 1050 / 675 / 0 |

## 结论

V28 完成了控制开销排查。当前没有更多 branch/HALT 控制泡泡可删，后续若要继续降周期，需要进入更深的指令集/流水线或多核任务拆分。
